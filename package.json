{
  "name": "darshgarg7-profile",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "GitHub profile assets and an evidence-first public activity visualization.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "generate": "node scripts/generate-ledger.mjs",
    "test": "node --test",
    "check:profile": "node scripts/check-profile.mjs",
    "check": "npm test && npm run generate -- --offline && npm run check:profile"
  }
}
